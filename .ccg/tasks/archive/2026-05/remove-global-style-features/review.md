# Review: 移除默认风格和常用风格功能

时间：2026-05-26T13:50:55Z

## 结论

通过本地审查。

## 变更检查

- 偏好设置已移除 `stylePrompt`，不再保存或追加全局风格要求。
- `projectBoards` / 常用风格相关状态、方法和持久化字段已移除。
- 设置页只保留通用生成参数：比例、质量、张数、格式、背景、审核、压缩率。
- 创作页、对话创作、历史页、详情页不再展示常用风格入口。
- 生成请求使用用户当前 prompt，不再通过默认风格二次拼接。
- 文档已记录 2026-05-26 的产品决策：默认风格 / 常用风格下线，后续方向转为任务队列、收藏服务端化、对话链聚合和显式参考图复用。

## 验证

- `rg` 源码残留搜索通过：核心前端文件和 `preferences` store 中没有 `stylePrompt`、`projectBoards`、`常用风格`、`默认风格` 等残留入口。
- `git diff --check` 通过。
- `npm run build` 通过，包含 Vite 前端构建和 Nest 后端构建。
- `npm run test --prefix server` 通过：5 个 test suite，17 个测试。

## 风险

- 旧浏览器 localStorage 中可能仍有 `stylePrompt` / `projectBoards` 旧字段，但新代码不会读取、展示或写回这些字段。
- 当前未新增迁移清理逻辑，因为这些字段只存在本地偏好存储，不影响生成链路。

## 外部审查

本轮使用本地构建、测试、残留搜索和 diff 检查完成审查。此前外部 Gemini / Claude wrapper 不可用，未能执行双模型审查。
