# Review

## Result
- `npm run build` passed.
- `git diff --check -- src/views/studio/HistoryDetailView.vue .ccg/tasks/fix-history-detail-header-ui/task.json` passed before build.

## Notes
- Replaced the oversized pill-like back button plus stacked chips with a compact information bar.
- The header now keeps back navigation, title, mode, ratio, and time in a single visual flow.
