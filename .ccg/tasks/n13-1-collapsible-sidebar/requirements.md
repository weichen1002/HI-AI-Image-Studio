# Requirements

按 `docs/goal-mode-roadmap-next.md` 的 N13.1 执行：

- `StudioView` 左侧菜单支持收起/展开。
- 收起状态刷新后保留。
- 收起状态只显示图标，并保留 title / aria-label。
- 管理中心支持二级菜单分组，不挤占普通功能区。
- 当前路由高亮准确。
- 移动端仍保持可用。

约束：

- 不提交、不归档。
- 使用 Codex 直接执行。
