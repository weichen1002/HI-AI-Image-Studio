# N12.2 后台导出能力

## Scope

- 用户、订单、积分流水、审计日志支持按当前筛选导出 CSV。
- 导出接口仅管理员可访问。
- 导出字段需要脱敏，不包含密码、密钥、完整 UA 等高敏字段。
- 大导出需要限制最大行数，并让前端按钮文案明确是导出当前筛选。

## Acceptance

- 后端提供四类 CSV 导出接口。
- 导出复用现有列表筛选条件。
- 单次导出最多 1000 行。
- CSV 正确转义逗号、引号、换行和公式注入前缀。
- 前端用户管理、订单、账务流水、审计日志页有导出按钮。
- 不提交、不归档。
